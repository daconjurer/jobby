# Jobs HTTP server (cmd/jobs-server)
APP_PORT=3001

# MongoDB Configuration (Single Database: jobby)
MONGODB_URI=mongodb://jobby_app:jobby_app_pass@localhost:27018/jobby?authSource=jobby
MONGODB_DATABASE=jobby
MONGODB_TIMEOUT=10s

# Collection Names (within jobby database)
MONGODB_COLLECTION_METADATA=job_metadata
MONGODB_COLLECTION_LOGS=job_logs

# Connection Pool Settings (Optional)
MONGODB_MAX_POOL_SIZE=100
MONGODB_MIN_POOL_SIZE=10

# For Docker Compose (service-to-service communication)
# MONGODB_URI=mongodb://jobby_app:jobby_app_pass@mongodb:27017/jobby?authSource=jobby
